// Mac Arabic keyboard layout (arabic-char → latin-key).
// Currently identical to Windows Arabic (101) for all core letter keys.
// Exported as a separate map so it can diverge independently when differences are confirmed.
export { windowsArabic as macArabic } from './windows-arabic';
